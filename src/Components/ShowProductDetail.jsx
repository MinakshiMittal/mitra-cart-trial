export const ShowProductDetail = ({ product }) => {
  return (
    <>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
      <h1>{product.price}</h1>
    </>
  );
};
